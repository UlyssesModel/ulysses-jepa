# CI for ulysses-jepa.
#
# Runs on every push and PR. Three jobs:
#   - test     pytest across 3.11 / 3.12, CPU only
#   - lint     ruff
#   - parse    AST-parse all .py files (fast canary; runs even if pip install fails)
#
# Targets are wired via the Makefile so local + CI commands match.

name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  parse:
    name: AST parse
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Parse every .py file
        run: |
          python3 - <<'PY'
          import ast, os, sys
          errors = []
          for root, dirs, files in os.walk("."):
              if any(skip in root for skip in (".git", "__pycache__", ".venv", "build", "dist")):
                  continue
              for f in files:
                  if f.endswith(".py"):
                      path = os.path.join(root, f)
                      try:
                          with open(path) as fh:
                              ast.parse(fh.read(), filename=path)
                      except SyntaxError as e:
                          errors.append(f"{path}: {e}")
          if errors:
              print(f"{len(errors)} syntax errors:")
              for e in errors:
                  print(f"  {e}")
              sys.exit(1)
          print("All Python files parse cleanly.")
          PY

  test:
    name: pytest (${{ matrix.python-version }})
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.11", "3.12"]
    steps:
      - uses: actions/checkout@v4
      - name: Clone Forward-Entropy-Benchmark (predictor abstraction)
        # Pinned to e2732baf0 — see DECISIONS.md D-012 + ADR-002. The
        # predictor repo has no packaging yet, so we put its scripts dir
        # on PYTHONPATH for tests. Update the SHA when D-012's "future
        # consolidation" path lights up upstream.
        uses: actions/checkout@v4
        with:
          repository: UlyssesModel/Forward-Entropy-Benchmark
          ref: e2732baf07b55aad32fec635d4c4fef9759518e9
          path: Forward-Entropy-Benchmark
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
          cache: pip
      - name: Install (CPU torch + dev extras)
        run: |
          python -m pip install --upgrade pip
          # Install CPU-only torch index first to avoid pulling CUDA wheels.
          pip install torch --index-url https://download.pytorch.org/whl/cpu
          pip install -e ".[scotty,kafka,serve,dev]"
          # Forward-Entropy-Benchmark's runtime deps that aren't already
          # transitively pulled in by ulysses-jepa.
          pip install pandas pyarrow requests
          # Skip [llm] in CI — transformers + accelerate is heavyweight and
          # the LLM-loading code paths are stub-tested elsewhere.
      - name: Run pytest
        run: |
          PYTHONPATH=src:.:${{ github.workspace }}/Forward-Entropy-Benchmark/scripts \
            pytest tests -v --tb=short \
            --ignore=tests/test_kirk_pipeline_integration.py
          # The kirk-pipeline integration test is skip-conditional on having
          # the production wheel; explicitly ignore its discovery in CI.

  lint:
    name: ruff
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
      - name: Install ruff
        run: pip install ruff
      - name: Lint
        run: ruff check src eval scripts tests
