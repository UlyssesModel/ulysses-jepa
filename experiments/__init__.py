"""Experiment-specific utilities.

This package holds modules that exist to support a particular experimental
program rather than the production pipeline. Per ADR-003, the first
inhabitant is `regime_generator`, which produces synthetic tensor streams
for the narrow Wonderwall experiment (B vs C on stationary vs strong
regime shift).
"""
